
import * as ACTION from "../actions/root-action";


export const reducer = (state, action) => {

    switch (action.type) {

        case "":
            return state;

        default:
            return state;
    }
};

